# Allowed origins for CORS (comma separated)
ALLOWED_ORIGINS=http://localhost:3000

# Default repository for synchronization and querying
DEFAULT_REPO_URL=https://github.com/Priyasharma620064/contextmesh

# Server port (default is 8080)
PORT=8080
